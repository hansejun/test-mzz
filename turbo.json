{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "globalEnv": ["IMPORTANT_GLOBAL_VARIABLE"],
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["TEACHER_URL", "STUDENT_URL"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
